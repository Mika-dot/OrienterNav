<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Tailscale encrypts the tunnel, while the local OrienterNet endpoint
         intentionally listens over HTTP on a dynamic 100.x address. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
