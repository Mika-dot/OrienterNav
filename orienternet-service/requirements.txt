# FastAPI service
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
httpx==0.28.1
Pillow==11.0.0

# OrienterNet runtime dependencies
numpy>=1.26,<3
opencv-python>=4.9
matplotlib>=3.8
plotly>=5.20
scipy>=1.11
omegaconf==2.3.0
hydra-core>=1.3
pytorch-lightning>=2.1
lightning-utilities>=0.10
torchmetrics>=1.0
timm>=1.0
imageio>=2.34
rtree>=1.2
lxml>=5.0
scikit-learn>=1.4
geopy>=2.4
exifread>=3.0
urllib3>=2
requests>=2.31
pandas>=2.2
shapely>=2.0
pyproj>=3.6
aiolimiter>=1.1

# Tests
pytest==8.3.4

# IMPORTANT:
# 1. CUDA-enabled torch/torchvision/torchaudio are installed separately by
#    INSTALL_WINDOWS.ps1 because the correct PyTorch index depends on CUDA.
# 2. maploc (OrienterNet) and perspective2d (PerspectiveFields) are external
#    editable packages. INSTALL_WINDOWS.ps1 clones, patches and installs them.
# 3. OpenSfM is not required for inference and is intentionally omitted because
#    its Windows build requires Visual Studio C++ Build Tools and NMake.
