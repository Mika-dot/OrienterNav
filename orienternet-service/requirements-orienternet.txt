# Full runtime used by the real OrienterNet backend.
# Install requirements.txt first, then install CUDA PyTorch separately, then
# install this file and the two editable repositories.

-r requirements.txt

# Configuration / model runtime
omegaconf==2.3.0
hydra-core>=1.3,<1.4
pytorch-lightning>=2.1,<3
torchmetrics>=1.0
lightning-utilities>=0.10
timm>=1.0
yacs>=0.1.8

# Vision and numeric stack
opencv-contrib-python>=4.9
albumentations>=1.4
matplotlib>=3.8
plotly>=5.20
scipy>=1.11
scikit-learn>=1.4
imageio>=2.34
pyequilib==0.3.0

# Geospatial / OSM stack
rtree>=1.2
lxml>=5.0
geopy>=2.4
pyproj>=3.6
shapely>=2.0
pandas>=2.2
requests>=2.31
urllib3>=2
aiolimiter>=1.1
exifread>=3.0

# torch, torchvision and torchaudio are intentionally not pinned here.
# INSTALL_WINDOWS.ps1 installs a CUDA build from the official PyTorch index.
