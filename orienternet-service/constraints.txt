# Optional compatibility constraints for the tested Windows/Python 3.11 setup.
# Do not install this file by itself.
#
# CUDA PyTorch is installed separately by INSTALL_WINDOWS.ps1 from the
# official PyTorch wheel index. Keeping torch out of this file prevents pip
# from silently replacing the CUDA build with a CPU-only or incompatible build.

numpy==1.26.4
omegaconf==2.3.0
pyequilib==0.3.0
